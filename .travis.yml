language:
  python

services:
  - docker

python:
  - 3.6

sudo: required

env:
  global:
    - K8S_OPS_REPO_BRANCH=master
    - K8S_OPS_REPO_SLUG=datahq/deploy
    - DOCKER_IMAGE=datopian/factory
    - DEPLOY_YAML_UPDATE_FILE=values.auto-updated.yaml
    - DEPLOY_VALUES_CHART_NAME=factory
    - DEPLOY_VALUES_IMAGE_PROP=image
    - DEPLOY_COMMIT_MESSAGE="automatic update of dhq-factory"
    - DEPLOY_GIT_EMAIL=dhq-deployer@null.void
    - DEPLOY_GIT_USER=dhq-deployer

script:
  - openssl aes-256-cbc -K $encrypted_f31baa052f19_key -iv $encrypted_f31baa052f19_iv -in datasets/core/config.json.enc -out datasets/core/config.json -d
  - openssl aes-256-cbc -K $encrypted_f31baa052f19_key -iv $encrypted_f31baa052f19_iv -in datasets/datahq/config.json.enc -out datasets/datahq/config.json -d
  - openssl aes-256-cbc -K $encrypted_f31baa052f19_key -iv $encrypted_f31baa052f19_iv -in datasets/sports-data/config.json.enc -out datasets/sports-data/config.json -d
  - curl -s https://raw.githubusercontent.com/datahq/deploy/master/apps_travis_script.sh > .travis.sh
  - bash .travis.sh script

deploy:
  skip_cleanup: true
  provider: script
  script: bash .travis.sh deploy
  on:
    branch: master
